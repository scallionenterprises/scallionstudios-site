Adding images
========
Use `scripts/make-small-images.sh [assets dir]` to convert all images to a small shape. Then when writing the markdown, use the includes to show the image with a link to the bigger image. example: `{% include product-image.html file="front.jpg" %}`
